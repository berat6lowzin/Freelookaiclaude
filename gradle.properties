# Fabric Properties
minecraft_version=1.21.1
loader_version=0.19.3
loom_version=1.17-SNAPSHOT

# Mod Properties
mod_version=1.0.0
maven_group=com.example
archives_base_name=freelook

# yarn_mappings and fabric_version are appended automatically by the
# GitHub Actions workflow right before the build, so they always match
# whatever is currently newest for minecraft_version above.
